{
  "name": "noop2",
  "label": "Noop 2 (TypeScript)",
  "description": "A basic TypeScript component executed with Deno",
  "documentation": "This component validates configuration in setup and then passes data through on execute.",
  "icon": "circle-off",
  "color": "orange",
  "configuration": [
    {
      "name": "message",
      "label": "Message",
      "type": "string",
      "description": "Message included in the emitted payload",
      "required": true,
      "default": "hello from noop2"
    },
    {
      "name": "includeInput",
      "label": "Include input payload",
      "type": "boolean",
      "description": "Whether to include the incoming input in the output payload",
      "required": false,
      "default": true
    }
  ],
  "output_channels": [
    {
      "name": "default",
      "label": "Default",
      "description": ""
    }
  ],
  "example_output": {
    "ok": true,
    "message": "hello from noop2"
  }
}
