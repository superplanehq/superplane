kind: Secret
metadata:
  name: api-credentials
  canvasId: my-application-canvas  # Reference to the canvas this secret belongs to
spec:
  provider: local  # Currently only local provider is supported
  local:
    api-key: "your-api-key-here"
    api-secret: "your-api-secret-here"
    # You can add any key-value pairs here
