/*
 * Fixed width so status badges align across rows
 * (9rem: enough for long labels at 10px;
 * narrower than the old 11rem).
 */
export const RUNS_CONSOLE_BADGE_COL = "flex w-36 flex-shrink-0 items-center justify-start gap-1.5";