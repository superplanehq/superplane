import { Layers } from "lucide-react";
import { ComingSoonPage } from "./ComingSoonPage";

export function LinesPage() {
  return (
    <ComingSoonPage
      title="Lines"
      description="Specialize how work moves through the workspace. Each phase runs an app."
      Icon={Layers}
    />
  );
}
