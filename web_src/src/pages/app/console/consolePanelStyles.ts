/** Console grid cards use the raised semantic surface in every theme. */
export const CONSOLE_PANEL_SHELL_SURFACE = "bg-surface-raised";

/** Inner body regions share the same semantic surface as the card shell. */
export const CONSOLE_PANEL_BODY_SURFACE = "bg-surface-raised";
