/** Subtle table header border — matches widget table body row dividers. */
export const CONSOLE_TABLE_HEAD_BORDER_DARK_CLASSES = "border-edge-subtle";

/** Uppercase caption typography shared by widget table headers and number panel labels. */
export const CONSOLE_WIDGET_LABEL_CLASSES = "text-[11px] font-semibold uppercase tracking-wide text-content-secondary";

/** Widget/data table column header typography + borders. */
export const CONSOLE_WIDGET_TABLE_HEAD_CLASSES = `border-b border-edge-default px-3 py-1.5 ${CONSOLE_WIDGET_LABEL_CLASSES}`;
