Goal: Remove "Pin App" from homepage - keep only star

## Change

Remove the "Pin App" action from the homepage. Keep only the star functionality.

## Priority

P3


Analyze the SuperPlane codebase and the workflow goal: Remove "Pin App" from homepage - keep only star

## Change

Remove the "Pin App" action from the homepage. Keep only the star functionality.

## Priority

P3

Write a clear, step-by-step implementation plan to a Markdown file named `plan.md` at the repository root (or update it if revising).

The plan must include:

1. A short summary of the intended change
2. Ordered implementation steps
3. Which files will change and why
4. How to verify the change (relevant `make` targets, packages, or UI checks)
5. Risks / out-of-scope notes

Do not implement code in this stage — planning only. Prefer reading existing patterns in the repo over inventing new ones.